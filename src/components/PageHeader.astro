---
/**
 * PageHeader — the <h1> block for every interior page. Exactly one per page
 * (§10.3), which is why it owns the h1 and Section never does.
 */
import RuyiCloud from './brand/RuyiCloud.astro';

interface Props {
  title: string;
  eyebrow?: string;
  intro?: string;
}
const { title, eyebrow, intro } = Astro.props;
---

<header class="bg-topo relative overflow-hidden border-b border-ink-600/60">
  <RuyiCloud
    side="right"
    class="pointer-events-none absolute -right-10 -top-16 h-40 w-auto opacity-25 sm:h-52"
  />
  <div class="relative mx-auto max-w-7xl px-4 py-14 sm:px-6 lg:py-20">
    {eyebrow && <p class="eyebrow mb-3">{eyebrow}</p>}
    <h1 class="max-w-4xl text-5xl text-bone sm:text-6xl lg:text-7xl">{title}</h1>
    {intro && <p class="mt-5 max-w-2xl text-base leading-relaxed text-bone-muted sm:text-lg">{intro}</p>}
  </div>
</header>
